# Reusable Workflows Repository

Personal reusable GitHub Actions workflows.

## Conventions

- Write usage examples as comments before the `jobs:` section in workflow files, not in README.md
- Include required `permissions` in usage examples (caller must set these for reusable workflows)
